# Data

This folder holds dataset material used by ShiroNet.

## Structure
- `raw/`: downloaded source datasets (gitignored)
- `processed/`: cleaned/split datasets for training (gitignored)

## Notes
Use `python -m src.data.prepare_dataset` to build processed splits from raw inputs.